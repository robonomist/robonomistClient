# Metadata of table

`data_metadata()` returns metadata information about a specific data
table. The returned metadata structure depends on the datasource.

## Usage

``` r
data_metadata(id, lang = NULL)
```

## Arguments

- id, :

  Exact table id

- lang, :

  Two-letter language code, e.g. "en" or "sv".

## Value

List of metadata
