# Create a robonomist_datasources object for tidy printing

Create a robonomist_datasources object for tidy printing

## Usage

``` r
as_robonomist_datasources(x)
```

## Arguments

- x:

  A data.frame with columns `id`, `title`, and `langugages`
